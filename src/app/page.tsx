import LoginForm from '@/auth/login/LoginForm';
import React from 'react';

const HomePage = () => {
    return (
        <div>
            <LoginForm/>
        </div>
    );
};

export default HomePage;